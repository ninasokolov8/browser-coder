{
  "name": "browser-coder",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "start": "NODE_ENV=production node server.mjs",
    "preview": "vite preview",
    "test": "cd tests && bash run-tests.sh all",
    "test:security": "cd tests && bash run-tests.sh security",
    "test:languages": "cd tests && bash run-tests.sh languages",
    "test:stress": "cd tests && bash run-tests.sh stress",
    "test:features": "cd tests && bash run-tests.sh features"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jszip": "^3.10.1",
    "monaco-editor": "^0.52.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.8"
  }
}
